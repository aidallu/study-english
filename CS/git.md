commit

한글 뜻: 약속하다, (범죄를) 저지르다

코딩에서 뜻: Git에서 변경사항을 저장하는 행위

영영사전

To do something illegal or considered wrong (일반)

In Git, a "commit" is a record of changes made to the repository, representing a snapshot of the project at a particular point in time. (코딩)

status

한글 뜻: 상태, 지위, 상황

코딩에서 뜻: Git에서 현재 작업 디렉토리와 스테이징 영역의 상태를 보여줌

영영사전

The current state or condition of someone or something. (일반)

A Git command that displays the state of the working directory and staging area. (코딩)

push

한글 뜻: 밀다, 추진하다

코딩에서 뜻: 로컬 저장소의 커밋을 원격 저장소에 업로드

영영사전

To move something away from oneself by pressing against it. (일반)

In Git, "push" means sending committed changes from a local repository to a remote repository. (코딩)

pull

한글 뜻: 끌다, 당기다

코딩에서 뜻: 원격 저장소의 변경사항을 로컬로 가져와 병합

영영사전

To move something toward oneself by holding it and exerting force. (일반)

In Git, "pull" means fetching changes from a remote repository and merging them into the current branch. (코딩)

config (configuration)

한글 뜻: 배치, 구성, 형태

코딩에서 뜻: Git 또는 시스템에서 사용자 이름, 이메일, 에디터 등 설정을 지정하는 명령어

영영사전

The arrangement of parts or elements in a particular form, figure, or combination. (일반)

In computing, "configuration" refers to the setup of a system or application, often controlled by settings or parameters. (코딩)

add

한글 뜻: 더하다, 추가하다

코딩에서 뜻: 변경된 파일을 Git의 스테이징 영역에 올리는 것

영영사전

To join something to something else to increase the size, number, or amount. (일반)

In Git, "add" means adding changes from the working directory to the staging area in preparation for a commit. (코딩)

rm (remove)

한글 뜻: 제거하다, 없애다

코딩에서 뜻: Git이나 터미널에서 파일을 삭제하는 명령어

영영사전

To take something away from, out of, or off the place where it is. (일반)

In Git, "rm" removes files from the working tree and from the index (staging area). (코딩)

init (initialize)

한글 뜻: 시작하다, 초기화하다

코딩에서 뜻: 새로운 Git 저장소를 생성하는 명령어

영영사전

To begin or put into operation for the first time. (일반)

In Git, "init" creates an empty repository or reinitializes an existing one. (코딩)

log

한글 뜻: 통나무, (행위·시간의) 기록, 일지

코딩에서 뜻: Git에서 커밋 이력을 보여주는 명령어, 또는 시스템 기록 파일

영영사전

An official record of events during a journey, in a system, or in daily activities. (일반)

In computing, "log" refers to a file that records events, processes, or changes, often used for debugging. (코딩)

ignore

한글 뜻: 무시하다, 신경 쓰지 않다

코딩에서 뜻: .gitignore 파일을 통해 특정 파일이나 폴더를 Git이 추적하지 않도록 지정

영영사전

To intentionally not listen to, look at, or give attention to something or someone. (일반)

In Git, "ignore" means to exclude specific files or directories from being tracked by version control. (코딩)
